enum FullTextQueryType {
  simpleQueryString = 'simple_query_string',
  match = 'match',
  prefix = 'prefix',
  fuzzy = 'fuzzy',
  term = 'term',
  queryString = 'query_string',
}

export default FullTextQueryType
