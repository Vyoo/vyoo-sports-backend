/**
 * Default disposal function implementation - calls `dispose` method on a given target
 * @param {Function | Object} obj disposal target
 */
const dispose = (obj: any): void => obj.dispose()

export default dispose
